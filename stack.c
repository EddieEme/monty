#include "monty.h"

/**
 * _stack - converts a list to a stack
 * @head: pointer to first node
 * @line_number: line_number
 * Return: nothing
 */
void _stack(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
	bus.lifi = 0;
}

